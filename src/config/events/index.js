export const eventStrategies = {
  delegated: {
    label: "Delegated",
    hint: "one listener on the container, attached once",
  },
  per_row: {
    label: "Listener per row",
    hint: "attach one listener to every row, every render",
  },
};
